# Design Spec — Лендинг «ИИ Рекрутер: калькулятор экономии найма»

One-page лендинг. Клиент за 30 секунд: выбирает отрасль → размер компании → видит экономию в ₽/год и часах → CTA «Получить пилот». Файл — `index.html`, тёмная тема, без сборки.

---

## 1. Общая концепция

Продающая «машина», а не утилита: минимум контролов, максимум цифр. Лендинг ведёт клиента по линейному сценарию **выбор → расчёт → доверие → действие**, без боковых веток и настроек. Настроение — спокойная уверенность Just AI: тёмный фон, индиго-акцент, зелёный для позитивных метрик, без игривости. Главный визуальный зацеп — **большая hero-цифра экономии** с count-up анимацией; остальное — поддержка.

Принципы:
- **Одна мысль на экране.** Каждая секция — один вывод.
- **Цифры крупнее текста.** Любая метрика — крупнее любого подзаголовка.
- **Hover и focus одинаково читаемы.** Все интерактивы доступны с клавиатуры.
- **Обновление результата мгновенное** (< 100ms), без лоадеров.

---

## 2. Палитра

База — из `calculator-internal.html`. Расширяем только под лендинг.

| Токен | HEX | Назначение |
|---|---|---|
| `--bg` | `#0f1419` | основной фон страницы |
| `--surface` | `#1a2332` | карточки, панели |
| `--surface-2` | `#243044` | вложенные блоки, вторичные карточки |
| `--surface-3` | `#2d3a50` | hover-состояние, active chips |
| `--border` | `#334155` | границы карточек |
| `--text` | `#f1f5f9` | основной текст, цифры |
| `--muted` | `#94a3b8` | подписи, дисклеймеры |
| `--accent` | `#6366f1` | CTA, активный выбор, ссылки |
| `--accent-light` | `#818cf8` | hover-акцент, градиент |
| `--accent-dim` | `rgba(99,102,241,.12)` | фон подсветки выбранного |
| `--success` | `#34d399` | позитивные метрики (экономия, лучший канал) |
| `--success-dim` | `rgba(52,211,153,.12)` | фон hero-метрики, «лучший» карточек |
| `--warning` | `#fbbf24` | оговорки, дисклеймеры |
| `--danger` | `#f87171` | негативные сценарии, ошибки |

**Дополнительные оттенки (для иконок отраслей и фонов карточек отраслей)** — подобраны так, чтобы оставаться в палитре и не выглядеть как детские ярлычки:
- IT/Digital — `#6366f1` (accent)
- Ритейл — `#f59e0b` (тёплый янтарь, между warning и success)
- Производство — `#71717a` (нейтральный металлик)
- Продажи — `#22d3ee` (cyan, дерзкий, в духе B2B)
- Финансы — `#3b82f6` (blue, серьёзный)
- Медицина — `#14b8a6` (teal, спокойный профессиональный)
- Строительство — `#a16207` (тёплый ochre)
- Логистика — `#8b5cf6` (violet, между accent и фоновым)

**Градиенты:**
- Hero-фон: `radial-gradient(ellipse at top, rgba(99,102,241,.18) 0%, transparent 55%)` поверх `--bg` — даёт «свет сверху», не давит.
- Hero-цифра экономии: `linear-gradient(135deg, #fff 0%, var(--accent-light) 100%)` — премиальный градиент на самой крупной метрике.
- Лучшая карточка в сравнении каналов: `linear-gradient(135deg, rgba(52,211,153,.16), rgba(52,211,153,.04))`.

---

## 3. Типографика

**Семейство:** системный sans-serif (без подгрузки, мгновенный рендер):
`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif`

**Шкала** (clamp для плавного масштабирования):

| Уровень | Размер | Weight | Line-height | Где |
|---|---|---|---|---|
| Display (hero-цифра) | `clamp(3rem, 9vw, 5.5rem)` | 800 | 1.0 | Hero-метрика экономии |
| H1 | `clamp(2.2rem, 5vw, 3.4rem)` | 800 | 1.1 | Заголовок Hero |
| H2 | `clamp(1.6rem, 3.2vw, 2.2rem)` | 700 | 1.15 | Заголовки секций |
| H3 | `clamp(1.15rem, 1.8vw, 1.4rem)` | 600 | 1.2 | Подзаголовки карточек |
| Body | `1rem` (16px) | 400 | 1.55 | Описания, подписи |
| Caption | `0.85rem` | 500 | 1.4 | Мелкие подписи, дисклеймеры |
| Eyebrow | `0.72rem` uppercase, letter-spacing .06em | 700 | 1.3 | Подзаголовки-маркеры («СРАВНЕНИЕ», «СЦЕНАРИИ») |

**`font-variant-numeric: tabular-nums`** обязательно для:
- всех денежных значений,
- процентов,
- счётчиков (вакансии, отклики, часы),
- CPA.

`letter-spacing: -0.02em` для H1, H2, hero-цифры — крупный шрифт плотнее смотрится.

---

## 4. Секции (сверху вниз)

7 секций + footer. Каждая — отдельная полоса с `padding: clamp(48px, 8vh, 96px) 0`.

### Секция 1 — Hero

**Цель:** продать визит дальше по странице, дать мгновенный контекст «зачем тут быть».

**Контент:**
- Eyebrow: `AI RECRUITER · РАСЧЁТ ЭКОНОМИИ ЗА 30 СЕКУНД`
- H1: «Сколько вы переплачиваете за найм, который можно отдать AI».
- Подзаголовок (body, muted): «Выберите отрасль и размер компании — покажем экономию в рублях и часах вашего рекрутера».
- CTA-кнопка: «Узнать экономию» — primary, accent, scroll к секции 2 (`#configure`). Под ней мелкая ссылка-скролл «или прокрутите вниз».

**Визуальный зацеп (под H1, занимает ~40% hero-сетки):**
- Слева — текстовый блок (60% ширины на desktop).
- Справа — **анимированная плашка-«калькулятор»**: полупрозрачная карточка с фоновыми KPI (статичные «средние по рынку РФ 2026»): «~2.5 млн ₽ в год уходит на найм одной вакансии в IT», «~180 часов рекрутера на 1 вакансию». Цифры — muted, слегка размытые (`filter: blur(0.5px)`), дрейфующие при наведении (лёгкий parallax на mousemove). Это **фоновый крючок**, не интерактив.

**Hover/анимации:**
- CTA при hover: фон светлеет (`--accent-light`), лёгкий `transform: translateY(-1px)`, тень `0 8px 24px rgba(99,102,241,.35)`.
- Параллакс-карточка: ±6px по X/Y в зависимости от положения курсора (через CSS-переменные `--mx`, `--my`).
- При появлении в viewport: fade-up 600ms ease-out, последовательно для eyebrow → H1 → subtitle → CTA → карточка.

**На mobile/tablet:** карточка справа скрывается (только текст), CTA остаётся крупным.

---

### Секция 2 — Конфигуратор (отрасль + размер)

**Цель:** два клика — получить расчёт. Без ввода чисел, без регионов, без сценариев.

**Структура:** один блок, два под-блока вертикально (на desktop — двухколоночный grid 1:1, на tablet/mobile — стек).

**Под-блок A — «Ваша отрасль» (8 карточек):**
- Грид 4×2 на desktop (≥1024px), 2×4 на tablet, 1×8 на mobile.
- Карточка: `padding: 18px 16px`, `--surface-2` фон, `--border` рамка, `--radius: 12px`.
- Содержимое карточки:
  - Цветной «значок» 40×40px сверху — квадрат с `border-radius: 10px` и фоновым оттенком отрасли (из таблицы выше), внутри — крупная эмодзи `font-size: 1.4rem`. (Эмодзи как временная мера; в проде заменить на SVG 24×24.)
  - Название отрасли (H3) ниже.
  - Мелкая подпись muted: «~60 откликов / вакансию» — средний показатель, чтобы клиент узнал свою реальность.
- **Состояния:**
  - Default: `--surface-2`, тонкая border.
  - Hover: `--surface-3`, border `--accent` (`transition: all 0.2s`).
  - Active/selected: фон `--accent-dim`, border `--accent`, лёгкий glow `0 0 0 3px rgba(99,102,241,.15)`, иконка чуть крупнее `transform: scale(1.05)`. Галочка-чек в правом верхнем углу (SVG 16×16, `--success`).
- `role="radio"` + `aria-checked`, навигация стрелками.

**Под-блок B — «Размер компании» (5 сегментов):**
- Switcher-чипы в один ряд (на mobile — wrap на 2 строки).
- Чип: `padding: 10px 18px`, `--surface-2`, `--border`, label из 2 строк (`Микро` / `1-15 чел.`).
- **Состояния:** аналогично карточкам (hover → accent border, selected → accent-dim фон + accent border + чек).
- Это segmented control, не radio (визуально и по UX).

**Логика отображения результата:**
- До выбора обоих параметров: на месте hero-метрики (секция 3) — плейсхолдер-карточка muted «Выберите отрасль и размер, чтобы увидеть экономию» с тонкой штриховкой `border-style: dashed`.
- Как только оба выбраны → мгновенный расчёт + fade-in результата (300ms).

**Микро-анимации:**
- Выбор карточки/чипа — `transform: scale(0.97)` 80ms при нажатии (тактильный фидбек).
- При выборе отрасли — под-блок B подсвечивается короткой пульсацией border (`@keyframes pulseBorder 600ms`).

---

### Секция 3 — Hero-метрика результата

**Цель:** одна цифра, которая остаётся в голове. Это главный «удар» лендинга.

**Структура:** один мощный блок по центру, `max-width: 980px`.

**До выбора (плейсхолдер):** muted-карточка с dashed-рамкой и текстом «Выберите отрасль и размер компании выше — здесь появится ваша экономия». Мелкая стрелка-подсказка «↑ наверх» если проскроллил мимо конфигуратора.

**После выбора:**

Сверху — eyebrow «ВАША ЭКОНОМИЯ С AI RECRUITER», uppercase, accent-light.

Центр — **hero-цифра**:
- Размер `clamp(3rem, 9vw, 5.5rem)`, weight 800, градиент `linear-gradient(135deg, #fff 0%, #818cf8 100%)`, `font-variant-numeric: tabular-nums`.
- Форматирование: «**12,4 млн ₽**» с разделителями разрядов, «/год» muted-серым `font-size: 0.5em` рядом.
- Под цифрой — поясняющая строка (body, muted): «против найма через КЦ и штатного рекрутера».

Под цифрой — **count-up анимация** при первом появлении (от 0 до значения за 1200ms, `ease-out`). При повторном изменении параметров — без анимации, мгновенно.

Ниже — **сетка 3-4 второстепенных KPI** (1×4 на desktop, 2×2 на tablet/mobile):
- «Часов рекрутера в месяц: **184**» (зелёный)
- «Релевантных кандидатов в месяц: **38**» (зелёный)
- «Стоимость пакета AI: **620 000 ₽**» (нейтральный)
- «Тарифицируемых минут: **22 000**» (нейтральный)

Каждый KPI — карточка `--surface-2`, padding 16px 20px, label сверху muted, value крупно (1.4rem, weight 700).

**Edge case «всегда плюс»:** если расчёт даёт отрицательное значение или около нуля — не показываем «выгоду», а переключаем фрейминг: «ИИ Рекрутер окупается за **X месяцев**» + «Стоимость закрытия вакансии: **Y ₽** (vs **Z ₽** по рынку)». Спецификация для кодера: в `data.js` или формуле должен быть гарантированный запас на edge cases (см. секцию 7).

**Hover/анимации:**
- Hero-цифра: лёгкое «дыхание» — `text-shadow: 0 0 32px rgba(99,102,241,.25)` пульсирует 4s infinite.
- KPI-карточки при hover: `transform: translateY(-2px)`, border `--accent`.

---

### Секция 4 — Ваша ситуация сейчас vs с ИИ Рекрутер

**Цель:** показать «до/после» через воронку + цифры, чтобы клиент понял масштаб изменений.

**Структура:** двухколоночный grid 1:1 на desktop, стек на mobile.

**Левая колонка — «Сейчас» (приглушённая, `--surface` без акцента):**
- Eyebrow «В ГОД БЕЗ AI».
- Воронка из 5 шагов (горизонтальные бары, как в референсе):
  1. Вакансий: **X**
  2. Откликов: **Y**
  3. Скрининг-интервью: **Z**
  4. Финальных интервью: **N**
  5. Наймов: **M**
- Каждый шаг: label слева, бар с `--surface-3` фоном и серым градиентом, число справа tabular-nums.
- Под воронкой — два muted-показателя: «~1 240 часов ручного труда в год» и «Стоимость закрытия: **180 000 ₽/вакансию**».

**Правая колонка — «С ИИ Рекрутер» (выделена `--success-dim` фоном, тонкая зелёная рамка):**
- Eyebrow «С AI RECRUITER», цвет `--success`.
- Те же 5 шагов, но с обновлёнными значениями + зелёные бары (`--success` градиент).
- Под воронкой — два зелёных показателя: «~120 часов ручного контроля в год» (в 10× меньше) и «Стоимость закрытия: **45 000 ₽/вакансию**» (в 4× дешевле).
- Мелкая подпись внизу: «*AI делает первичный контакт и скрининг, рекрутер подтверждает финалистов*».

**Анимации:**
- Бары воронки: `width: 0 → value%` 800ms ease-out с задержкой 100ms между шагами (stagger).
- При смене параметров — fade-cross 200ms (исчезает старое, появляется новое).

---

### Секция 5 — Сравнение каналов (CPA)

**Цель:** визуально доказать, что ИИ Рекрутер дешевле на релевантного кандидата.

**Структура:** сетка из 4 карточек в ряд (на desktop), 2×2 на tablet, вертикальный стек на mobile.

Карточки: «Ручной рекрутер», «Колл-центр», «Сценарный бот», **«ИИ Рекрутер»** (последняя — выделенная).

**Содержимое каждой карточки:**
- Название канала (H3).
- Главная метрика — **CPA** (₽ за 1 релевантного кандидата), крупно (1.8rem, weight 700, tabular-nums).
- Под ней — мелкая подпись «конверсия X%», «₽Y/мин».
- Мини-бар «эффективность»: длина бара пропорциональна CPA (длиннее = дороже).
- Иконка-эмодзи или простой символ (📞 для КЦ, 🤖 для бота, 👤 для ручного, ⚡ для AI).

**Лучшая карточка (ИИ Рекрутер):**
- Фон `linear-gradient(135deg, rgba(52,211,153,.16), rgba(52,211,153,.04))`.
- Рамка `rgba(52,211,153,.5)`.
- Бейдж «✓ ЛУЧШИЙ CPA» сверху справа, фон `--success`, цвет `--bg`, `padding: 4px 10px`, `font-size: 0.7rem`, `border-radius: 999px`, weight 700.
- Число CPA — цвет `--success`.

**Сравнительная подпись под сеткой:**
- Строка muted: «ИИ Рекрутер дешевле ручного найма в **3,8×**, КЦ — в **2,1×**, сценарного бота — в **5,4×**».
- Множители — weight 700, `--success`.

**Анимации:**
- Бары эффективности — `width` анимация 600ms ease-out с stagger 80ms.
- Лучшая карточка — лёгкий `transform: scale(1.02)` на старте (300ms), затем норма.
- Hover на любой карточке — `transform: translateY(-4px)`, тень `0 12px 32px rgba(0,0,0,.3)`.

---

### Секция 6 — Сценарии экономии (vs альтернативы)

**Цель:** показать выгоду в трёх разных сравнениях, а не только CPA.

**Структура:** три крупные карточки в ряд (на desktop), стек на mobile.

**Карточка 1 — «vs Штатный HR»:**
- Eyebrow muted «СРАВНЕНИЕ».
- H3 «Против найма через своего рекрутера».
- Контент: таблица-блок из 3 строк:
  - «Зарплата рекрутера: **80 000 ₽/мес**» (зачёркнуто, muted)
  - «ИИ Рекрутер: **~50 000 ₽/мес** за пакет»
  - «**Экономия: 360 000 ₽/год**» (крупно, `--success`)

**Карточка 2 — «vs Колл-центр»** (тоже muted-оформление, не «лучшая»):
- H3 «Против аутсорс-колл-центра».
- «КЦ: **17 ₽/мин** × Y минут = **Z ₽/год**»
- «ИИ Рекрутер: пакет X ₽/год»
- «**Экономия: N ₽/год**»

**Карточка 3 — «vs SaaS-альтернативы»:**
- H3 «Против ИИ-SaaS».
- «SaaS: **149 ₽/интервью** × K интервью = **M ₽/год**»
- «ИИ Рекрутер: пакет X ₽/год»
- «**Экономия: N ₽/год**»

**Визуально:** карточки `--surface-2`, padding 24px 24px, border-radius 14px. Строка «Экономия» внизу — на фоне `--success-dim`, padding 12px 16px, border-radius 10px, число крупно (1.5rem, tabular-nums).

**Дополнительно — общий итог под карточками:**
- Блок «**Суммарная экономия по трём сценариям: ~X млн ₽/год**», центрирован, H2, градиент-цифра как в hero-метрике.

**Hover:** все карточки `translateY(-2px)`, border `--accent`.

---

### Секция 7 — Социальное доказательство + CTA «Получить пилот»

**Цель:** закрыть возражения «а кто уже пользуется?» и перевести к действию.

**Структура:** сначала узкий блок доказательства (1 экран), затем крупный CTA-блок (1 экран).

**Блок доказательства (компактный):**
- Eyebrow «ПОЧЕМУ ЭТО РАБОТАЕТ».
- H2 «Что показал пилот на холодной базе».
- Сетка 3 цифр (1×3 на desktop, стек на mobile):
  - «**26,3%** — попыток заканчиваются контактом» (с рынком КЦ 8-12%)
  - «**50,1%** — контактов переходят в содержательное интервью»
  - «**27,1%** — интервью заканчиваются релевантным кандидатом»
- Под цифрами — мелкая подпись muted: «Данные пилота ИИ Рекрутер на холодной базе, N>1000 контактов».
- ⚠️ Не «логотипы клиентов» — нет публичных кейсов под NDA (см. user profile: Just AI под NDA, без кейс-стади). Лучше честная статистика, чем фейковые лого.

**CTA-блок «Получить пилот»:**
- Контрастный фон: `linear-gradient(180deg, rgba(99,102,241,.18) 0%, rgba(99,102,241,.04) 100%)`.
- H2: «Готовы увидеть пилот на своих вакансиях?».
- Подзаголовок: «Оставьте контакт — пришлём расчёт под вашу отрасль и предложим формат пилота на 2-4 недели».
- **Форма** (без бэкенда — `mailto:` или просто копирование в буфер + сообщение):
  - Поле «Email или Telegram» (input, full-width на mobile).
  - Кнопка primary «Получить пилот» — крупная, padding 18px 32px.
  - Под кнопкой — мелкий дисклеймер muted: «Не передаём контакты третьим лицам. Ответим в течение рабочего дня».
- Альтернативно: «или напишите нам на **sales@just-ai.com**» ссылкой.

**Анимации:**
- Цифры доказательства — count-up при появлении в viewport.
- CTA-кнопка при hover — лёгкая пульсация glow (`box-shadow` keyframes 2s infinite).

---

### Footer (минимальный)

- Одна строка: логотип Just AI (текстом, weight 700) слева, «© 2026 Just AI» по центру, ссылка «sales@just-ai.com» справа.
- Фон `--surface`, верхняя граница `--border`, padding 24px 0.
- Никаких ссылок на соцсети (нечего ставить).

---

## 5. Микро-интеракции и анимации

**Глобально:**
- Все transition: `transition: all 0.2s ease` (стандарт), 0.3s для крупных блоков.
- `prefers-reduced-motion: reduce` — все анимации сокращаются до 0.01ms (a11y).

**При скролле (Intersection Observer):**
- Секции fade-up при попадании в viewport (`opacity: 0; transform: translateY(20px)` → `opacity: 1; transform: translateY(0)`, 600ms ease-out).
- Stagger 80ms для KPI-карточек, баров воронки, цифр доказательства.
- Запускается один раз, не повторяется.

**Hover (универсальные правила):**
- Primary-кнопки: `transform: translateY(-1px)` + glow shadow.
- Карточки: `transform: translateY(-2px)` + border-color → `--accent` (или `--success` для «лучших»).
- Чипы/радио: border-color → `--accent`, фон `--accent-dim`.
- Cursor: `pointer` на всём кликабельном.

**Focus (a11y):**
- `outline: 2px solid var(--accent-light); outline-offset: 2px` на всех интерактивах.
- `:focus-visible` (только с клавиатуры), не `:focus`.

**Обновление результата:**
- При смене отрасли/размера — мгновенный пересчёт (<100ms).
- Цифры в hero-метрике — fade-cross 200ms (исчезают за 100ms, появляются за 200ms) при ЛЮБОМ изменении.
- Count-up — только при первом появлении результата из плейсхолдера (один раз).

**CTA-кнопки:**
- Active (нажатие): `transform: scale(0.97)` 80ms — тактильный фидбек.

**Параллакс в Hero:**
- Карточка-«калькулятор» справа: `transform: translate(calc(var(--mx) * 6px), calc(var(--my) * 6px))`, обновляется через JS на mousemove (desktop only, отключать на touch).

---

## 6. Responsive поведение

### Desktop (>1024px)
- Hero: двухколоночный 60/40.
- Конфигуратор: двухколоночный 1:1 (отрасль / размер).
- KPI-сетка после hero-метрики: 1×4.
- Сравнение каналов: 1×4.
- Сценарии экономии: 1×3.
- Контейнер: `max-width: 1240px`, центрирован, `padding: 0 32px`.

### Tablet (768-1024px)
- Hero: одноколоночный (карточка-калькулятор скрывается).
- Конфигуратор: вертикальный стек.
- Отрасли: грид 2×4.
- KPI: 2×2.
- Каналы: 2×2.
- Сценарии: 1×3 (компактнее).
- `padding: 0 24px`.

### Mobile (<768px)
- Все секции — вертикальный стек.
- Отрасли: 1×8 (вертикальный список карточек, можно 2×4 в зависимости от длины имён).
- Размер компании: чипы с wrap.
- Hero-цифра: уменьшается до `clamp(2.4rem, 11vw, 3.4rem)`.
- Каналы: 1×4 стек.
- Сценарии: 1×3 стек.
- CTA-форма: full-width, кнопка full-width.
- `padding: 0 16px`.
- Размеры шрифтов — через `clamp` уже учтены.

### Общее
- На tablet/mobile отключить: parallax, count-up (или заменить на простое появление), уменьшить stagger до 40ms.
- На mobile hover-эффекты — `transform` оставить, `:hover` → `:active` (touch).

---

## 7. Edge cases и защита от негатива

### «Всегда плюс» — стратегия фрейминга

Если расчёт по формуле даёт «выход в ноль» или слабо-положительный результат (например, micro-компания IT с 1.8 вакансий/год), переключать фрейминг:

1. **Показать ROI по стоимости закрытия**, а не по общей экономии:
   - «Стоимость закрытия 1 вакансии: **X ₽** (vs **Y ₽** по рынку)»
2. **Показать окупаемость**:
   - «ИИ Рекрутер окупается за **N наймов**» (вместо «экономия в год»)
3. **Сравнить с альтернативами**, даже если vs «ничего» не выгодно:
   - «vs SaaS — экономия **M ₽**» (SaaS-сценарий обычно положительный)
4. **Подчеркнуть не-денежные выигрыши**:
   - «Часов ручного труда сэкономлено: **N**»
   - «Релевантных кандидатов в месяц: **K**»

Запрещено показывать **отрицательную экономию** красным — это отпугивает. Если данных не хватает — не показывать блок, а показать «Свяжитесь для индивидуального расчёта».

### Низкая конверсия / малый объём откликов (Медицина, IT)

Медицина (35 откликов/вакансию, conv 7.13%) и IT (60 откликов, conv 2.38%) — разные профили боли:
- Медицина: «мало откликов, но высокая конверсия» → акцент на «расширяете воронку за счёт AI-обзвона».
- IT: «много откликов, но теряются в скрининге» → акцент на «AI отфильтрует 90% шума за вас».

Если значение отрасли показывает мало вакансий (micro), подпись в hero-метрике мягко переключается: «при вашем объёме AI окупится за **2-3 месяца пилота**».

### Пустой выбор

Если клиент только зашёл и ничего не выбрал:
- Hero-метрика — плейсхолдер (см. секцию 3).
- Секции 4-5-6 — данные не подгружаются, но структура видна с placeholder-значениями «—» в muted-цвете.
- CTA в форме — disabled, текст «выберите параметры выше», или сразу разрешать отправку «Получить пилот под вашу отрасль» (без расчёта).

### Большие числа (enterprise 3000+ чел.)

- Форматирование: «2,5 млн ₽», «12,8 млн ₽», «1,2 млрд ₽» (для enterprise).
- Корректное склонение: «миллион / миллиона / миллионов», «тысяча / тысячи / тысяч». Библиотека — `numRu` или встроенная функция.

### Отказ от JS / медленный интернет

- Базовый layout рендерится без JS (HTML+CSS).
- При отключённом JS — кнопка CTA ведёт на `mailto:sales@just-ai.com?subject=Пилот%20AI%20Recruiter`.

### Мобильный Safari, Firefox, Edge

- Только `position: sticky` если используется — без него.
- `clamp()` поддерживается везде с 2021.
- `prefers-reduced-motion` — обязательно.

---

## 8. Acceptance чеклист (для кодер-воркера)

- [ ] Палитра и токены — копия раздела 2.
- [ ] Шрифт системный, без подгрузки.
- [ ] 7 секций + footer, в указанном порядке.
- [ ] Конфигуратор — карточки отраслей 4×2 desktop + чипы размера 5 в ряд.
- [ ] Hero-метрика — count-up 1200ms при первом расчёте, fade-cross при смене параметров.
- [ ] Сравнение каналов — 4 карточки, ИИ Рекрутер выделен success-рамкой и бейджем «ЛУЧШИЙ CPA».
- [ ] Сценарии — 3 карточки (ручной/КЦ/SaaS).
- [ ] CTA-форма в финале — input + кнопка + дисклеймер.
- [ ] Footer — одна строка, лого Just AI, copyright, контакт.
- [ ] Responsive: 3 брейкпоинта, на mobile всё в один столбец, parallax отключён.
- [ ] A11y: `role="radio"` на карточках отраслей, focus-visible outline, aria-labels на форме, keyboard-навигация.
- [ ] Edge cases: нет отрицательных значений, нет «—» при отсутствии данных (плейсхолдер), числа >1M форматируются как «X млн».
- [ ] `<script src="data.js">` подключён, нет inline-копии данных.
- [ ] Нет npm, нет сборки, открывается двойным кликом по `index.html`.
- [ ] Размер JS < 100KB, нет внешних библиотек.
- [ ] Нет placeholder Lorem Ipsum — все тексты подставляются из `copy.md`.

---

## 9. Что НЕ делать

- ❌ Не дублировать `calculator-internal.html` с кучей параметров. Это не утилита.
- ❌ Не использовать эмодзи для логотипа компании в hero (только в карточках отраслей как временная мера).
- ❌ Не показывать отрицательные значения экономии — лучше сменить фрейминг.
- ❌ Не вставлять фейковые логотипы клиентов — нет публичных кейсов (NDA).
- ❌ Не использовать ярлыки вроде «Революция в найме», «AI заменит рекрутера», «убийца HH» — без игривости, Just AI-voice.
- ❌ Не отрасль «Другое» — 8 отраслей жёстко из `data.js`.
- ❌ Не модальные окна, не попапы, не прелоадеры — лендинг линеен.
- ❌ Не видео, не автоплей, не звуки.
- ❌ Не тёмный режим / светлый режим — одна тема.
